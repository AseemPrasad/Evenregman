# App
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Auth.js / NextAuth
AUTH_URL=http://localhost:3000
AUTH_SECRET=

# MongoDB Atlas
MONGODB_URI=
MONGODB_DB=evenregman

# Redis (Optional - for atomic capacity reservation)
REDIS_URL=
ENABLE_ATOMIC_REGISTRATIONS=false

# Outbox Pattern (Optional - for async event processing)
ENABLE_OUTBOX_PATTERN=false
OUTBOX_RELAY_ENABLED=false
OUTBOX_POLL_INTERVAL_MS=5000
OUTBOX_MAX_RETRIES=5
OUTBOX_MAX_RETRY_DELAY_MS=300000

# Rate Limiting (Optional - for abuse protection)
ENABLE_RATE_LIMITING=false
RATE_LIMIT_STRICT_MODE=false

# Async CSV Export (Optional - for background export processing)
# ENABLE_ASYNC_EXPORTS: Enable async export feature (returns 202 Accepted)
# ASYNC_EXPORTS_WORKER_ENABLED: Run background worker process
ENABLE_ASYNC_EXPORTS=false
ASYNC_EXPORTS_WORKER_ENABLED=false

# S3/R2 Storage (Required if async exports enabled)
# S3_BUCKET_NAME: S3 bucket name (e.g., "evenregman-exports")
# S3_REGION: AWS region or "auto" for Cloudflare R2
# AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY: AWS credentials for S3 access
S3_BUCKET_NAME=
S3_REGION=auto
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Enterprise RBAC/ABAC & Audit (Optional - for multi-tenant access control)
# ENABLE_RBAC_ENGINE: Enable role-based access control system
# RBAC_AUTO_BOOTSTRAP_ORGS: Auto-create personal orgs on first login
# RBAC_AUDIT_RETENTION_DAYS: How long to keep audit logs (default: 2555 = 7 years)
# RBAC_LOG_SENSITIVE_ACTIONS: Log VIEW_SENSITIVE actions separately
ENABLE_RBAC_ENGINE=false
RBAC_AUTO_BOOTSTRAP_ORGS=true
RBAC_AUDIT_RETENTION_DAYS=2555
RBAC_LOG_SENSITIVE_ACTIONS=false

# Two-Tier Edge & L2 Distributed Cache (Optional - for performance)
# ENABLE_L2_CACHE: Enable Redis L2 caching for public events
# CACHE_TTL_EVENTS: Cache TTL for single event (default: 3600s = 1 hour)
# CACHE_TTL_EVENTS_LIST: Cache TTL for event list (default: 300s = 5 min)
# CACHE_COMPRESSION_ENABLED: Enable gzip compression for large values
# CACHE_COMPRESSION_THRESHOLD_BYTES: Only compress if > this size (default: 1024)
ENABLE_L2_CACHE=false
CACHE_TTL_EVENTS=3600
CACHE_TTL_EVENTS_LIST=300
CACHE_COMPRESSION_ENABLED=true
CACHE_COMPRESSION_THRESHOLD_BYTES=1024

# Enterprise SAML 2.0 / OIDC Single Sign-On & SCIM (Optional - for enterprise SSO)
# ENABLE_ENTERPRISE_SSO: Enable SAML/OIDC/SCIM identity provider support
# SAML_STRICT: Enforce strict SAML assertion validation (cert, issuer, binding)
# OIDC_TIMEOUT_SECONDS: Timeout for OIDC token exchange (default: 30s)
# SCIM_RATE_LIMIT: Max SCIM requests per minute from each org (default: 100)
# SESSION_REVOCATION_ENABLED: Enable JWT session revocation on user deprovisioning
# SESSION_REVOCATION_TTL_HOURS: How long to keep revocation info in memory (default: 168 = 1 week)
ENABLE_ENTERPRISE_SSO=false
SAML_STRICT=true
OIDC_TIMEOUT_SECONDS=30
SCIM_RATE_LIMIT=100
SESSION_REVOCATION_ENABLED=true
SESSION_REVOCATION_TTL_HOURS=168

# Full-Stack Distributed Tracing & OpenTelemetry (Optional - for observability)
# ENABLE_OTEL_TRACING: Enable distributed tracing via OpenTelemetry (default: false)
# OTEL_EXPORTER_OTLP_ENDPOINT: OTLP collector endpoint (e.g., http://localhost:4318)
# OTEL_SERVICE_NAME: Service name in traces (default: evenregman)
# OTEL_SERVICE_VERSION: Service version for versioning traces (default: 1.0.0)
# OTEL_SAMPLE_RATE: Trace sampling rate 0.0-1.0 (default: 1.0 = 100%)
# OTEL_BATCH_PROCESSOR_ENABLED: Use batch processor for efficient export (default: true)
# OTEL_BATCH_SPAN_PROCESSOR_SCHEDULE_DELAY: Batch export interval ms (default: 5000)
ENABLE_OTEL_TRACING=false
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
OTEL_SERVICE_NAME=evenregman
OTEL_SERVICE_VERSION=1.0.0
OTEL_SAMPLE_RATE=1.0
OTEL_BATCH_PROCESSOR_ENABLED=true
OTEL_BATCH_SPAN_PROCESSOR_SCHEDULE_DELAY=5000

# Resilient External Service Circuit Breaker & Fallback (Optional - for resilience)
# ENABLE_CIRCUIT_BREAKER: Enable circuit breaker for external service calls (default: false)
# CIRCUIT_BREAKER_TIMEOUT_MS: Individual request timeout (default: 3000ms)
# CIRCUIT_BREAKER_ERROR_THRESHOLD_PERCENTAGE: Trip threshold (default: 50%)
# CIRCUIT_BREAKER_VOLUME_THRESHOLD: Min requests before evaluation (default: 20)
# CIRCUIT_BREAKER_RESET_TIMEOUT_MS: Recovery probe interval (default: 30000ms = 30s)
# CIRCUIT_BREAKER_ROLLING_WINDOW_MS: Failure rate evaluation window (default: 10000ms)
# CIRCUIT_BREAKER_ENABLE_FALLBACK: Enable graceful fallback to queue (default: true)
# CIRCUIT_BREAKER_FALLBACK_QUEUE_RETENTION_DAYS: Queue item TTL (default: 7)
ENABLE_CIRCUIT_BREAKER=false
CIRCUIT_BREAKER_TIMEOUT_MS=3000
CIRCUIT_BREAKER_ERROR_THRESHOLD_PERCENTAGE=50
CIRCUIT_BREAKER_VOLUME_THRESHOLD=20
CIRCUIT_BREAKER_RESET_TIMEOUT_MS=30000
CIRCUIT_BREAKER_ROLLING_WINDOW_MS=10000
CIRCUIT_BREAKER_ENABLE_FALLBACK=true
CIRCUIT_BREAKER_FALLBACK_QUEUE_RETENTION_DAYS=7

# Real-Time Change Data Capture (CDC) Analytics Event Pipeline (Optional - for analytics)
# ENABLE_CDC_PIPELINE: Enable CDC worker for real-time analytics (default: false)
# CDC_PIPELINE_WORKER_ENABLED: Run CDC worker process (default: false)
# CDC_BATCH_SIZE: Events per batch before projection (default: 100)
# CDC_BATCH_INTERVAL_MS: Max wait before processing batch (default: 5000ms)
# CDC_RESUME_TOKEN_STORAGE: Storage backend for resume tokens (redis or mongodb, default: redis)
# CDC_ANALYTICS_COLLECTION: MongoDB collection for aggregated metrics (default: analytics_timeseries)
# CDC_ERROR_RETRY_DELAY_MS: Delay before retrying on connection failure (default: 5000ms)
# CDC_MAX_RESUME_TOKEN_AGE_SECONDS: Max age of resume token before restart (default: 604800 = 1 week)
ENABLE_CDC_PIPELINE=false
CDC_PIPELINE_WORKER_ENABLED=false
CDC_BATCH_SIZE=100
CDC_BATCH_INTERVAL_MS=5000
CDC_RESUME_TOKEN_STORAGE=redis
CDC_ANALYTICS_COLLECTION=analytics_timeseries
CDC_ERROR_RETRY_DELAY_MS=5000
CDC_MAX_RESUME_TOKEN_AGE_SECONDS=604800